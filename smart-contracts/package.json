{
  "name": "poolfi-smart-contracts",
  "version": "1.0.0",
  "description": "PoolFi Smart Contracts for Reef Network",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js --network reefPelagia",
    "deploy:simple": "hardhat run scripts/deploy-simple.js --network reefPelagia",
    "deploy:pelagia": "hardhat run scripts/deploy.js --network reefPelagia",
    "deploy:pelagia-poolmanager": "hardhat ignition deploy ./ignition/modules/PoolManager.js --network reefPelagiaTestnet",
    "deploy:pelagia-simple": "hardhat ignition deploy ./ignition/modules/SimplePoolManager.js --network reefPelagiaTestnet",
    "deploy:pelagia-basic": "hardhat ignition deploy ./ignition/modules/BasicPool.js --network reefPelagiaTestnet",
    "deploy:testnet": "hardhat run scripts/deploy.js --network reefTestnet",
    "node": "hardhat node"
  },
  "keywords": [
    "solidity",
    "reef",
    "defi",
    "pools",
    "savings"
  ],
  "author": "PoolFi Team",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@parity/hardhat-polkadot": "^0.1.9",
    "dotenv": "^16.3.1",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0"
  }
}
