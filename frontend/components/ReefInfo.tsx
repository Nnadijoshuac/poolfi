'use client'

import { useChainId } from 'wagmi'
import { isReefChain, isReefMainnet, isReefTestnet, getReefChainInfo } from '@/lib/reef-config'

export default function ReefInfo() {
  // Component removed as requested
  return null
}
